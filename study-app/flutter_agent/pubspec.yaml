name: flutter_agent
description: A local agent client based on OpenClaw's ReAct design patterns.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  openai_dart: ^0.5.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.0